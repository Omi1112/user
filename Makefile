TEST_FILES=$(shell find -name '*_test.go')
build: task
	go version
task:
	# １行で一つのシェルが動く そのため２行で以下をかくとgo testがmainのテストを行ってしまう。
	# 疑問これ、複数のテスト書いていったとして、最後のテストの合否判定で、テストの失敗成功判定されたりしないのかな？
	# 調べたら、circleciは終了コードが1かどうか判定しているらしい。makefileの終了コードを調整できる様にいろいろ調べて
	# 終了コードは、コマンド実行後に、「echo $?」で確認可能成功していれば0
	cd server; go test
	# エラーテストやってみたらmakeの実行がそこで終わるっぽいまぁテスト全部通せないけどいいのかな。。。
	# 下記を書く事で、異常終了と判定できるif文でうまいことやりたい。
	# exit 1

